// (x: number, y: number) -> number
function add(a, b) {
  return a + b;
}

// (x: number, y: number) -> number
function substract(a, b) {
  return a - b;
}

// (x: number, y: number) -> number
const mutiply = (a, b) => a * b;
